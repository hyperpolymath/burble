#!/bin/sh
# SPDX-License-Identifier: PMPL-1.0-or-later
# Generated by alloyiser — Alloy analysis script
# Requires: java, alloy.jar

ALLOY_JAR="alloy.jar"
MODEL="/var/mnt/eclipse/repos/burble/generated/alloyiser/burble.als"
SOLVER="sat4j"
TIMEOUT="300"

echo "=== alloyiser analysis ==="
echo "Model: $MODEL"
echo "Solver: $SOLVER"
echo "Assertions: 4"

echo "Checking: no_orphan_records..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "no_orphan_records"
echo ""

echo "Checking: room_membership_bounded..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "room_membership_bounded"
echo ""

echo "Checking: call_requires_authenticated_user..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "call_requires_authenticated_user"
echo ""

echo "Checking: media_track_owner_exists..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "media_track_owner_exists"
echo ""

echo "=== analysis complete ==="
